# Default values
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
microservice:
  name: nginx
  namespace: default
  env: prod
  image: nginx:latest
  PullPolicy: Always
  profile: docker
  type: ClusterIP
  replicas: 1
  host: peek-challenge.local
  
hpa:
  minReplicas: 1
  maxReplicas: 2